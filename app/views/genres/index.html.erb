<h1>Listing Genres</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
    </tr>
  </thead>

  <tbody>
    <% @genres.each do |genre| %>
      <tr>
        <td><%= link_to(genre.name, genre) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
