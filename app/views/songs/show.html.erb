<h1><%= @song.name %></h1>
  <dl>
    <dt>Artist</dt>
      <dd><%= link_to(@song.artist.name, artist_path(@song.artist)) %></dd>
    <dt>Genre</dt>
      <dd><%= link_to(@song.genre.name, genre_path(@song.genre)) %></dd>
  </dl>
