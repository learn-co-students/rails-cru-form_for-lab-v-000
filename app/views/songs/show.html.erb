<!-- display each of the song's genre and artist, and link to the respective genre and artist show pages. -->

<h1><%= @song.name %></h1>

<!--	<%= (link_to @song.artist.name, artist_path(@song.artist)) if @song.artist%><br>
	<%= (link_to @song.genre.name, genre_path(@song.genre)) if @song.genre%><br>

