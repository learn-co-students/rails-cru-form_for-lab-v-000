<h1><%=@song.name%></h1>
<!--#navigate
#  shows the name on the show page in a h1 tag (FAILED - 1)
#  to artist pages
#  shows the bio on the show page in a p tag (FAILED - 2)-->
<h4>Artist: <%= link_to @song.artist.name, artist_path(@song.artist)%></h4>
<h4>Genre: <%= link_to @song.genre.name, genre_path(@song.genre)%></h4>
