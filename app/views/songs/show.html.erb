<h1><%= @song.name %></h1>
<p>Genre: <%= link_to @song.genre.name, genre_path(@song.genre) if !@song.genre.nil? %></p>
<p>Artist: <%= link_to @song.artist.name, artist_path(@song.artist) if !@song.artist.nil? %></p>
