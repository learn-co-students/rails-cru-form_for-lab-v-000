<h1><%=@song.name%></h1>
<p><%=link_to @song.genre.name, genre_path(@song.genre)%>, by <%=link_to @song.artist.name, artist_path(@song.artist) %></p>

<p><%=link_to "Edit Song",edit_song_path(@song)%></p>
