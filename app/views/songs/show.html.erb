
<h1> Song Name: <%= @song.name %> </h1><br>

<h2> Artist Name:  <%= link_to @song.artist.name, artist_path(@song.artist)) if @song.artist%> </h2> <br>
<h2> Genre Name: <%= link_to @song.genre.name, genre_path(@song.genre)) if @song.genre %> </h2>
