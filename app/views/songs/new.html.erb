<h1>Song Form</h1>

<%=form_for(@song) do |f| %>

  <%= f.label :name %>:
  <%= f.text_field :name %><br>

  <%= f.text_field :artist_id %>
  <%= f.text_field :genre_id %>

  <%= f.submit "Create Song" %>

  <% end %>
