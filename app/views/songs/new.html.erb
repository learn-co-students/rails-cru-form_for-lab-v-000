<%= form_for(@song) do |f| %>
	
	<%= f.label :name %>
	<%= f.text_field :name %>

	<%= f.collection_select :artist_id, Artist.all, :id, :name %>
	<%= f.collection_select :genre_id, Genre.all, :id, :name %>

	<%= f.submit %>

<% end %>