<%= form_for :song, :url => url_for(:controller => 'songs', :action => 'create') do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.number_field :artist_id %>
  <%= f.number_field :genre_id %>
  <%= f.submit 'Create Song' %>
<% end %>
