<h1>Song Form</h1>

<%= form_for(@song) do |f| %>
	<label>Song: </label>
	<%= f.text_field :name %> <br>

	<label>Artist: </label>
	<%= f.text_field :name %><br>

	<label>Genre: </label>
	<%= f.text_field :name %><br>

	<%= f.submit %>

<% end %>
