<h1>All Songs</h1>

<% @songs.each do |song| %>
  <%= song.name %><br>
  <%= link_to song.genre.name, genre_path(song.genre) %><br>
  <%= link_to song.artist.name, artist_path(song.artist) %>
<% end %> 