<% @songs.each do |song| %>
	<p><%= (link_to song.artist.name, artist_path(song.artist)) if song.artist %> | <%= song.name %> | <%= (link_to song.genre.name, genre_path(song.genre)) if song.genre %></p>
<% end %>