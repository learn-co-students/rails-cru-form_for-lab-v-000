
<% @songs.each do |song|%>
   <p><%= song.name %></p>
    <%= link_to song.genre, genre_path(song.genre_id), id: "My Genre" %>
    <%= link_to song.artist, artist_path(song.artist_id), id: "My Artist" %>
<% end %>


