<h1>Songs</h1>

<% @songs.each do |song| %>
  <h4><%= song.name %></h4>
  <h6><%= link_to song.artist.name, artist_path(song.artist) %></h6>
  <h6><%= link_to song.genre.name, genre_path(song.genre) %></h6>
<% end %>
