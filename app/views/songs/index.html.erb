<% @songs.each do |song| %>
<p><%= song.name %> Genre:<%= link_to song.genre.name, genre_path(song.genre) %> Artist:<%= link_to song.artist.name, artist_path(song.artist) %></p>
<%= binding.pry %>
<% end %>
