<p>My Song</p>

<% @songs.each do |song| %>
  <%= link_to song.name, song_path(song) %><br>
  <%= link_to song.genre.name, genre_path(song.genre) %><br>
  <%= link_to song.artist.name, artist_path(song.genre) %><br>
<% end %>
