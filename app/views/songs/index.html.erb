<% @songs.each do |song| %>
	<li><%= link_to song.name, song_path(song) %> | <%= link_to song.genre.name, genre_path(song.genre) %> | <%= link_to song.artist.name, artist_path(song.artist) %></li>
<% end %>
