<h1><%= @artist.name %></h1>
<p><%= @artist.bio %></p>
  <dl>
    <dt>Songs</dt>
      <% @artist.songs.each do |song| %>
        <dd>-<%= link_to(song.name, song_path(song)) %></dd>
      <% end %>
  </dl>
