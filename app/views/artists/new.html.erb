<h1>Create Artist:</h1>
<%= form_for(@artist) do |f| %>
  <label>Name:</label>
  <%= f.text_field :name %> <br>

  <label>Bio:</label>
  <%= f.text_area :bio %>
  
  <%= f.submit %>
<% end %>
