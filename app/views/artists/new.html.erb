<h1>Create new artist</h1>

<%= form_for(@artist) do |f| %>
  <%= f.label :artist_name %>
  <%= f.text_field :name %>
  <%= f.label :bio %>
  <%= f.text_area :bio %>
  <%= f.submit "Create Artist" %>
  <% end %>