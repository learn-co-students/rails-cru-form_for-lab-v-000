<h1>New Artist Form</h1>

<%= form_for @artist do |f| %>
  <%= f.label "Artist Name:" %>
  <%= f.text_field :name %>
  <br>
  
  <%= f.label "Artist Bio:" %>
  <%= f.text_field :bio %> 
  <br>
  
  <%= f.submit %>
<% end %>