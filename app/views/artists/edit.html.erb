<%= form_for @artist do |f| %>
  <p>Create a new artist</p>
    <p>
      <%= f.label :name %><br>
      <%= f.text_field :name %>
    </p>
    <p>
      <%= f.label :bio %><br>
      <%= f.text_area :bio %>
    </p>
  <%= f.submit %>

<% end %>
