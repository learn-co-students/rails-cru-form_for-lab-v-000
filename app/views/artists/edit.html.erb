<h3>Artist Form</h3>

<%= form_for(@artist) do |f| %>
  <%= f.label :name, "Artist name:" %><br>
  <%= f.text_field :name %><br>

  <%= f.label :bio, "Artist bio:" %><br>
  <%= f.text_field :bio %><br>

  <%= f.submit %>
<% end %>