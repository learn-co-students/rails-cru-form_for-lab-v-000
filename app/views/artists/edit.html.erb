<h1>Edit <%= @artist.name %></h1>
<%= form_for(@artist) do |f| %>
  <label>Name: </label>
  <%= f.text_field :name %> <br></br>

  <label>Bio</label>
  <%= f.text_area :bio %> <br></br>

  <%= f.submit %>
<% end %>
