<h1> Edit an Artist </h1>

<h3> Artist Edit Form </h3>

<%= form_for(@artist) do |f| %>
  <p>
  <%= f.label :name %>
  <%= f.text_field :name %>
  </p>

  <p>
  <%= f.label :bio %>
  <%= f.text_area :bio%>
  </p>

  <%= f.submit %>
<% end %>
