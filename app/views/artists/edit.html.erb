<h2>Edit Artist</h2>

    <%= form_for(@artist) do |f| %>
        <label>Artist Name: </label>
        <%= f.text_field :name %>
        
        <label>Bio: </label>
        <%= f.text_area :bio %>

        <%= f.submit %>
    <% end %>