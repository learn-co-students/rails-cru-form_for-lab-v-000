exit
genre_path(@genre)
genres_path(@genre)
genres_path
@genre.save
@genre = Genre.new(strong_params(:name))
Genre.new(strong_params(:name))
exit
params
exit
  @artist = Artist.new(params.require(:artist).permit(:name, :bio))
@artist = Artist.new.require(:artists).permit(:name, :bio)
params
